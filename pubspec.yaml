name: block_dialog
description: "Block-based, animated dialogs for Flutter with composable layout, typed results, and fine-grained dismissal control."
version: 1.0.0
homepage: https://github.com/tripletroop/flutter_block_dialog
repository: https://github.com/tripletroop/flutter_block_dialog

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true